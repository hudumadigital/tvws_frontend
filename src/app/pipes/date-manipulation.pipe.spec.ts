import { DateManipulationPipe } from './date-manipulation.pipe';

describe('DateManipulationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateManipulationPipe();
    expect(pipe).toBeTruthy();
  });
});
